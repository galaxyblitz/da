function testOne(inputOne) {
  var valueOne = inputOne;

  if (valueOne >= 100) {
    var text = "You are equal to or greater than 100";
  } else {
    var text = "You are smaller than 100";
  }

  return text;
}
